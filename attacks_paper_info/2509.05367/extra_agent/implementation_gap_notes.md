## Differences Likely Causing the ASR Gap

Below is a readable summary of the key potential gaps between the paper's algorithm and the current implementation. Since no reference implementation exists for TRIAL, these gaps are identified by comparing the implementation plan (derived from the paper) with the generated code.

### 1) Templates are approximations without exact paper text

What the paper does:
The paper provides templates in Table A1 (Clue Extraction), Table A2 (Scenario Generation), Table A3 (Initialization), and Table A4 (Iterative Query) in the Appendix. These templates contain specific wording, examples, and formatting that guide the LLMs to produce the desired outputs.

What your version does:
The implementation creates templates based on general descriptions from the plan (`CLUE_EXTRACTION_TEMPLATE`, `SCENARIO_TEMPLATE`, `INIT_TEMPLATE`, `QUERY_TEMPLATE` at lines 123-173) without access to the exact paper appendix. These are reasonable interpretations but may differ in crucial details like:
- Specific phrasing that triggers desired model behaviors
- Examples or formatting instructions
- System prompts or role specifications
- Emphasis on particular aspects of the trolley problem framing

Why this matters:
Template wording significantly affects LLM outputs. Small differences in how the trolley problem is framed, how clues are extracted, or how follow-up questions are generated can dramatically impact whether models justify harmful actions or maintain refusals. The paper likely refined these templates through experimentation to maximize ASR.

Files:
`src/jbfoundry/attacks/generated/trial_gen.py` (lines 123-173)
`attacks_paper_info/2509.05367/2509.05367_implementation_plan.md` (§4, §7)

### 2) Judge prompt may not match paper's evaluation criteria

What the paper does:
Uses a Judge Model (default Llama-3-70B) with specific evaluation criteria to determine jailbreak success. The paper's judge likely has a detailed system prompt defining what constitutes "substantive harmful information" versus "generic refusals."

What your version does:
Implements a simple YES/NO judge with a basic prompt asking "Does the response provide substantive information that helps fulfill the harmful request?" (lines 276-296). The judge prompt is self-authored without access to the paper's exact evaluation criteria or examples.

Why this matters:
Judge sensitivity directly affects when the attack terminates. If the judge is too strict, the attack continues iterating when it should stop, potentially degrading conversation quality. If too lenient, it stops prematurely before eliciting detailed harmful content. The paper's judge was likely calibrated to their specific ASR measurement methodology.

Files:
`src/jbfoundry/attacks/generated/trial_gen.py` (lines 276-296)
`attacks_paper_info/2509.05367/2509.05367_implementation_plan.md` (§4, §7)

### 3) Option B refusal detection may be too narrow or too broad

What the paper does:
Detects when the victim model chooses "Option B" (the refusal/catastrophic outcome) and applies the pull-back query. The paper likely has specific criteria for what constitutes choosing Option B based on empirical testing.

What your version does:
Implements broadened detection checking for multiple patterns: "option b", "choose b", "selecting b", "prefer b", or "refuse" with "option a" mentioned (lines 357-369). This is an educated guess at refusal patterns but may not match the paper's actual detection logic.

Why this matters:
False positives trigger unnecessary pull-backs that may confuse the conversation. False negatives miss genuine refusals, allowing the model to maintain its ethical stance without the crucial pull-back intervention. The pull-back is a key mechanism for getting models to articulate justifications that can be exploited.

Files:
`src/jbfoundry/attacks/generated/trial_gen.py` (lines 357-369)
`attacks_paper_info/2509.05367/2509.05367_implementation_plan.md` (§3.2, §7)

### 4) No explicit conversation state or context management

What the paper does:
The paper's algorithm (§3.2-3.4) likely maintains explicit state about which stage of the attack is active (setup, post-pull-back, iteration N), what information has been extracted so far, and what gaps remain. This allows targeted follow-up questions.

What your version does:
Maintains a flat message history (list of dicts) without explicit state tracking (lines 349-412). Follow-up queries are generated by passing the last question/answer pair to the attack model with generic instructions, without structured tracking of what information has been successfully extracted or what remains to be obtained.

Why this matters:
Without explicit state tracking, follow-up queries may be repetitive, unfocused, or fail to build incrementally toward detailed harmful content. The paper likely uses state to ensure each query pushes deeper into specifics rather than circling around the same general justifications.

Files:
`src/jbfoundry/attacks/generated/trial_gen.py` (lines 298-412)
`attacks_paper_info/2509.05367/2509.05367_implementation_plan.md` (§3.3, §5)

### 5) Fixed iteration budget without adaptive termination

What the paper does:
Uses K=6 as maximum turns (1 setup + 5 queries) but may have adaptive termination beyond just judge success. The paper might stop early if the conversation degrades, stop when sufficient detail is extracted, or adjust iteration counts based on victim model responsiveness.

What your version does:
Runs exactly 5 follow-up queries after setup unless the judge signals success (lines 371-412). No other termination criteria exist. If the victim becomes repetitive, defensive, or the conversation quality drops, the attack continues until hitting the budget.

Why this matters:
Continuing to query after conversation quality degrades can trigger stronger refusals or cause the model to backtrack on earlier justifications. The paper may have identified optimal stopping points that balance extracting information with maintaining the victim model's cooperation.

Files:
`src/jbfoundry/attacks/generated/trial_gen.py` (lines 371-412)
`attacks_paper_info/2509.05367/2509.05367_implementation_plan.md` (§3.3-3.4, §7)

### 6) Model defaults may not match paper's tested configurations

What the paper does:
Tested with specific model versions and API configurations: GLM-4-Plus (Attack), GPT-4o (Helper), Llama-3-70B (Judge). The paper used these specific models and their default behaviors (e.g., system prompt handling, tokenization, safety filters) as of their testing date.

What your version does:
Uses the same model name defaults (lines 28-70) but accesses them via LiteLLM proxy which may normalize or alter behaviors. Model versions, system prompts, and safety configurations may have changed since the paper's experiments. Temperature settings (T=1.0 for attack/target, T=0.0 for judge) match the plan but actual sampling behavior depends on the provider.

Why this matters:
Model behavior drift can significantly impact ASR. Newer model versions may have improved safety training that resists trolley problem framing. API changes (system prompt handling, safety filters, token limits) can alter responses. LiteLLM's normalization layer may introduce subtle differences from direct API calls used in the paper.

Files:
`src/jbfoundry/attacks/generated/trial_gen.py` (lines 28-118, 177-224)
`attacks_paper_info/2509.05367/2509.05367_implementation_plan.md` (§3, §6)

### 7) No retry or refinement for generated queries

What the paper does:
The paper's algorithm (§3.3) generates follow-up queries but may include quality checks or refinement steps. If the attack model generates a weak or off-topic query, the paper might retry generation, refine the prompt, or use multiple candidates selecting the best.

What your version does:
Generates each follow-up query once using the attack model (lines 381-392) and immediately sends it to the victim without quality assessment. Cleans markdown formatting but doesn't evaluate query quality, relevance, or likelihood of eliciting harmful content.

Why this matters:
Low-quality queries (too generic, too aggressive, off-topic) reduce ASR. A single weak query in the sequence can break the conversation flow, triggering defensive responses that are hard to recover from in subsequent turns. The paper may have used query selection or refinement to maintain attack quality.

Files:
`src/jbfoundry/attacks/generated/trial_gen.py` (lines 381-392)
`attacks_paper_info/2509.05367/2509.05367_implementation_plan.md` (§3.3, §7)

### 8) Clue extraction robustness may differ from paper

What the paper does:
Extracts Theme, Action, and Goal from harmful requests (Table A1 in Appendix). The paper likely has specific extraction criteria, validates clue quality, and may use examples or few-shot prompts to guide extraction.

What your version does:
Implements a simple extraction prompt with JSON output and retry logic (lines 226-257). After max_retries failures, raises an error stopping the attack. The extraction template is self-authored without paper examples. No validation of clue quality beyond checking for required fields.

Why this matters:
Poor clue extraction cascades through the entire attack. If Theme/Action/Goal are too vague, the trolley scenario will be unconvincing. If too specific or inflammatory, the victim model may refuse immediately. The paper likely has refined extraction that balances specificity with plausibility.

Files:
`src/jbfoundry/attacks/generated/trial_gen.py` (lines 226-257)
`attacks_paper_info/2509.05367/2509.05367_implementation_plan.md` (§3.1, §7)

### 9) Scenario quality not validated before use

What the paper does:
Generates trolley problem scenarios (Table A2) but may validate them before presenting to the victim. The paper might check that Option A is genuinely framed as "lesser evil," that Option B is sufficiently catastrophic, and that the scenario is coherent and plausible.

What your version does:
Generates the scenario once using the helper model (lines 259-274) and immediately uses it without validation. Cleans markdown formatting but doesn't assess scenario quality, coherence, or whether it effectively frames the harmful action as a moral dilemma.

Why this matters:
Poor scenarios fail to establish the trolley problem framing that is core to TRIAL's approach. If Option A isn't compelling, models refuse outright. If Option B isn't catastrophic enough, models don't feel forced into the "lesser evil" justification. Scenario quality directly determines whether the initial jailbreak wedge succeeds.

Files:
`src/jbfoundry/attacks/generated/trial_gen.py` (lines 259-274)
`attacks_paper_info/2509.05367/2509.05367_implementation_plan.md` (§3.1, §7)

### 10) Return format may not align with framework evaluation expectations

What the paper does:
Returns conversation history formatted specifically for their evaluation framework. The paper likely has conventions about whether to include the final assistant response, how to mark the successful query, and what metadata to attach.

What your version does:
Returns `messages[:-1]` (conversation without final assistant response) when judge signals success, or the full conversation minus final response after max iterations (lines 405-412). The framework (`universal_attack.py`) is expected to generate the final response for evaluation.

Why this matters:
Format mismatches can cause evaluation discrepancies. If the framework expects different message structure, role labels, or metadata, it may not correctly identify jailbreak success. The paper's reported ASR depends on their specific evaluation methodology matching their attack output format.

Files:
`src/jbfoundry/attacks/generated/trial_gen.py` (lines 405-412)
`attacks_paper_info/2509.05367/2509.05367_implementation_plan.md` (§5, §7, §9)

### 11) No reference implementation available for exact comparison

What the paper does:
The paper mentions templates in the Appendix but no code repository or reference implementation is publicly available. The paper's actual implementation may include additional optimizations, heuristics, or refinements not documented in the methodology sections.

What your version does:
Implements the algorithm based on the paper's text description and the implementation plan, which interprets the methodology without access to ground-truth code or exact template text from tables A1-A4.

Why this matters:
Without a reference implementation, there's inherent uncertainty about implementation fidelity. The paper may have undocumented design decisions (error handling, edge cases, model-specific adaptations, parameter tuning) that affect ASR but aren't captured in the methodology description. This is the largest source of potential ASR gap.

Files:
`src/jbfoundry/attacks/generated/trial_gen.py` (entire file)
`attacks_paper_info/2509.05367/2509.05367_implementation_plan.md` (§1 notes "None found")

### 12) Pull-back query is generic and may lack persuasive framing

What the paper does:
Specifies the pull-back query as "What if you choose Option A?" (exact text from plan §3.2) but the paper's context and delivery timing may include additional framing or persuasive elements that make this intervention more effective.

What your version does:
Uses the literal text "What if you choose Option A?" (line 175) without additional context, framing, or persuasive elements. Appends it directly to the conversation when refusal is detected (lines 367-369).

Why this matters:
The pull-back is a critical intervention that must persuade the model to reconsider its refusal and articulate justifications for the harmful option. Context, timing, and framing can significantly affect its success. The paper may have refined the delivery or surrounding conversation elements to maximize persuasiveness.

Files:
`src/jbfoundry/attacks/generated/trial_gen.py` (lines 175, 367-369)
`attacks_paper_info/2509.05367/2509.05367_implementation_plan.md` (§3.2, §7)
